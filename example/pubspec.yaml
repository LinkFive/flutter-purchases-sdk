name: linkfive_purchases_example
description: Demonstrates how to use the linkfive_purchases plugin.
version: 0.0.1+1

publish_to: 'none'

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  linkfive_purchases: ^2.1.3
  #path: ../packages/linkfive_purchases
  linkfive_purchases_provider: ^2.1.3
    #path: ../packages/linkfive_purchases_provider
  in_app_purchases_paywall_ui: ^1.0.0
  #path: ../../in_app_purchases_paywall/packages/in_app_purchases_paywall_ui
  cupertino_icons: ^1.0.5
  provider: ^6.0.3
  flutter_bloc: ^8.0.1
  equatable: ^2.0.3

dependency_overrides:
  in_app_purchases_intl: ^1.0.0
  in_app_purchases_interface: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/apikey.json
